# Car Workshop management

## Description

A web application for management of your car workshop

## Dev steps

1. [uv](https://docs.astral.sh/uv/) is needed for ease of dev setup
2. after cloning run `uv sync` to setup venv
3. source venv
4. run `flask run --debug` to start up the dev server
